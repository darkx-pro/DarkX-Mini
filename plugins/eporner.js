// Similar to multi but only Eporner (more reliable direct mp4)
const axios = require('axios');

module.exports = {
    command: 'eporner',
    description: 'Search and download from Eporner (best quality)',
    category: 'downloader',
    execute: async (sock, m, { text, prefix, reply }) => {
        // Tumia logic sawa na ile ya multi lakini focus Eporner tu
        // ... (copy na modify kutoka multi au xnxx code yako)
    }
};